terraform plan
Creates an execution plan (shows what will change).

terraform apply
Applies the changes required to reach the desired state.

terraform apply -auto-approve
Applies changes without confirmation.

terraform destroy
Destroys all managed infrastructure.

